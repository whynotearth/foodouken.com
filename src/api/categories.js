// import axios from 'axios';

export default {
  getCategories() {
    // return axios.get('https://stagingapi.whynot.earth/api/v0/categories');
  },
  getCategory(category) {
    // return axios.get("https://stagingapi.whynot.earth/api/v0/pages/slug/foodouken/" + category);
    return category;
  }
};
